# Xeneon Dashboard Requirements
# This dashboard uses the existing FastAPI telemetry backend
# No additional Python dependencies required for basic functionality

# Optional: For enhanced development and testing
uvicorn[standard]>=0.18.0
python-dotenv>=1.0.0

# Frontend dependencies (handled via CDN in HTML)
# - No Python packages required for frontend
# - Uses CDN-hosted fonts (Google Fonts)
# - Pure HTML/CSS/JavaScript implementation